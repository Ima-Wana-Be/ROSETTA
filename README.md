Go to the generator:
https://ima-wana-be.github.io/ROSETTA/

To install on iPhone:
Safari → Share button → “Add to Home Screen” → it appears as an app icon, opens fullscreen with no browser chrome, works offline.

To install on Android:
Chrome will show an “Add to Home Screen” or “Install app” banner automatically. Or via the three-dot menu → “Add to Home Screen”.
